import DataProvider from './DataProvider'
import mount from 'enzyme'

describe("StateValidatorUnique", () => {
  it("should fail if positive value is not given", () => {
    let dataProvider = new DataProvider();
  });
it('should check url',() => {
  let _baseUrl = "https://swapi.co/api";
let url = 'people';
  let furl = `${this._baseUrl}/${url}`;
  //FirstName Change Event
  
  const wrapper = mount(<Provider store={store}><EcheckSale updateDetails={updateDetails} masterData={masterData} buyer={buyer} value={value} errors={errors} onChange={() => { }} /></Provider>)
  const childWrapper = wrapper.find(EcheckSale);
  childWrapper.instance().onChange(nameChangeEvent);
})
})